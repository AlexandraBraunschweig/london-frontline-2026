{
  "name": "thanet-evacuation-deck",
  "private": true,
  "description": "Slide deck. reveal.js is vendored into vendor/ so the deck needs no install to present; this manifest only records which version, for a future upgrade. See README.md.",
  "devDependencies": {
    "reveal.js": "6.0.1"
  }
}
